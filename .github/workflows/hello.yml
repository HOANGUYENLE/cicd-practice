name: Hello World

on:
 push:

jobs: 
  say-hello: 
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello from GitHub Actions!"

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Python
        run: python --version

      - name: Run Python
        run: python main.py

    